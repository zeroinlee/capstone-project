import 'package:flutter/material.dart';

/// 여기에 개발하시면 됩니다.
class AuthPage extends StatelessWidget {
  const AuthPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Center(child: Text('Auth Page')));
  }
}
